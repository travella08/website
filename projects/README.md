# Projects folder

Drop project PDFs here (e.g. `my-project.pdf`), then add an entry
in `js/main.js` pointing to it:

```js
{
  title: "My Project",
  description: "One or two sentences on what it does.",
  tags: ["Math", "CS"],
  links: [{ label: "PDF", url: "projects/my-project.pdf" }],
},
```
